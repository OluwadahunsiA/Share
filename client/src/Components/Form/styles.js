import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  formBox: {
    width: '350px',
  },
  formPaper: {
    padding: '10px',

  },
  formText: {
    display: 'block',
    padding: '30px',
    textAlign: 'center',
  },
  formField: {
    width: '100%'
  },
});
